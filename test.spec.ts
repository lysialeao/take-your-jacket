import { render } from "react-dom"


describe('Take your jacket test', () => {
    it("It must contain an input for reading the name of the city", () => {

    })
    it("It must contain a button to send the information", () => {
        
    })
    it("The request must be made with the name of the city as a parameter", () => {
        // fazer o mock da função 
    })
})